name: build and push docker images

on:
  push:
    branches: [main]
    paths:
      - 'microservices/**'
      - 'smithy/**'
      - 'Dockerfile.service'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/build-push.yml'
  workflow_dispatch:
    inputs:
      tag:
        description: image tag (default git short SHA)
        required: false

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1

jobs:
  build-and-push:
    name: build ${{ matrix.service }}
    # Solo corre cuando la infra de AWS esta lista y el rol OIDC esta
    # configurado en el secret AWS_DEPLOY_ROLE. Hasta entonces, los push a
    # main no disparan builds (los manuales via workflow_dispatch sirven
    # como override para forzar la corrida).
    if: vars.AWS_CI_ENABLED == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - problems-service
          - users-service
          - submissions-service
          - contests-service
          - executor-service
    steps:
      - uses: actions/checkout@v4

      # packages/ está gitignored; los SDKs Smithy se generan y compilan aquí
      # para que el Dockerfile los pueda copiar.
      - name: setup Java (Smithy CLI requires JVM)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.3.0

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      - name: install Smithy CLI
        run: |
          curl -L https://github.com/smithy-lang/smithy/releases/download/1.71.0/smithy-cli-linux-x86_64.zip -o /tmp/smithy.zip
          unzip -q /tmp/smithy.zip -d /tmp/smithy
          sudo /tmp/smithy/smithy-cli-linux-x86_64/install
          smithy --version

      - name: smithy build (genera SDKs + OpenAPI)
        run: pnpm --filter @leetcode/smithy run smithy:build

      - name: build SDKs TS
        run: pnpm -r --filter '@leetcode/*-sdk' run build

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: ECR login
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: set image tag
        id: tag
        run: |
          if [ -n "${{ github.event.inputs.tag }}" ]; then
            echo "tag=${{ github.event.inputs.tag }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT
          fi

      - name: set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: build and push (standard service)
        if: matrix.service != 'executor-service'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.service
          build-args: |
            SERVICE=${{ matrix.service }}
          push: true
          tags: |
            ${{ steps.ecr.outputs.registry }}/leetcode/${{ matrix.service }}:${{ steps.tag.outputs.tag }}
            ${{ steps.ecr.outputs.registry }}/leetcode/${{ matrix.service }}:latest
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}

      - name: build and push (executor)
        if: matrix.service == 'executor-service'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: microservices/executor-service/Dockerfile
          push: true
          tags: |
            ${{ steps.ecr.outputs.registry }}/leetcode/executor-service:${{ steps.tag.outputs.tag }}
            ${{ steps.ecr.outputs.registry }}/leetcode/executor-service:latest
          cache-from: type=gha,scope=executor-service
          cache-to: type=gha,mode=max,scope=executor-service
